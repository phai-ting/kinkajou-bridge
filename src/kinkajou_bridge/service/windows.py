from __future__ import annotations


def run_service_cli() -> int:
    """Placeholder for Windows Service install/start/stop commands."""
    print(
        "Windows service mode is scaffolded but not fully implemented yet.\n"
        "For now run: kinkajou-bridge          (system tray + API)\n"
        "         or: kinkajou-bridge --service  (API only, no tray)"
    )
    return 1
